def info(sub, domain)

    dig_result = dig_output
    info = awk_dst

if domain:
    for sub in subs:
        dest = dest(sub, domain):x

        print("sub points to dest")

else:
    dest = dest(sub, domain)
    print("subn points to dest)
